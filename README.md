# EuropeanFootball

archive.zip file contains all the leagues that are there in Europe

File is in zip format so the first step is unzip the file and save it

Next Step is using ADF extract all the files present

Combined_matches.csv holds the main data. Use League column to create partitions and store in parquet format
